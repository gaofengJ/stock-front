import React from 'react';
import RouterConfig from '@/routes';
import './App.less';

const App: React.FC = () => {
  return (
    <div>
      <RouterConfig />
    </div>
  );
};

export default App;
